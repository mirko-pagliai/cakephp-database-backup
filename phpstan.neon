includes:
    - vendor/cakedc/cakephp-phpstan/extension.neon

parameters:
    level: 8

    paths:
        - config
        - src
        - tests

    bootstrapFiles:
        - tests/bootstrap.php

    treatPhpDocTypesAsCertain: false

    excludePaths:
        -  tests/test_app/
